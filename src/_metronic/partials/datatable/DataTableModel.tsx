import { TypeData } from "../../enum/type-data";

export interface DataTableModel{
    value: string,
    dataType: TypeData
}
export interface DataTableSource{
    value: string,
    dataType: TypeData
}